version: '3.8'

services:
  web:
    build: ./main_app
    ports:
      - "8000:8000"
    volumes:
      - ./main_app:/app  # Mapeia o diretório correto
    environment:
      - DJANGO_SETTINGS_MODULE=setup.settings
      - PYTHONPATH=/app

  deck:
    build: ./deck_app
    ports:
      - "8001:8000"
    volumes:
      - ./deck_app:/app  # Mapeia o diretório correto
    environment:
      - DJANGO_SETTINGS_MODULE=setup.settings
      - PYTHONPATH=/app
